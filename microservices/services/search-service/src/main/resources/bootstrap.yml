spring:
  application:
    name: ${xcolab.namespace:xcolab}-search-service
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  jmx:
    default-domain: ${spring.application.name}
